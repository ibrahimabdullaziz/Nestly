openapi: 3.0.3
info:
  title: Roovia API
  description: OpenAPI specification for the Roovia rental marketplace backend.
  version: 1.0.0
  contact:
    name: Ibrahim Abdullaziz
    email: ibrahimabdullaziz55@gmail.com
  license:
    name: Apache 2.0
    url: https://www.apache.org/licenses/LICENSE-2.0

servers:
  - url: http://localhost:2005
    description: Local development server

security:
  - bearerAuth: []

tags:
  - name: Auth
  - name: Catalog
  - name: Units
  - name: Bookings
  - name: Reviews
  - name: Favorites

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  responses:
    BadRequest:
      description: Invalid request payload or parameters.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Unauthorized:
      description: Authentication required or invalid token.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Forbidden:
      description: Action not allowed for the current role or ownership.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    NotFound:
      description: Resource was not found.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Conflict:
      description: Resource conflict or duplicate.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    InternalServerError:
      description: Unexpected server error.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"

  schemas:
    Error:
      type: object
      required:
        - status
        - message
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: Invalid credentials

    User:
      type: object
      required:
        - id
        - email
        - firstName
        - lastName
        - role
      properties:
        id:
          type: string
          format: uuid
          example: 7d08f7c4-9d6b-4e12-bfdf-0f8e0b6f0ee8
        email:
          type: string
          format: email
          example: guest@example.com
        firstName:
          type: string
          example: Jane
        lastName:
          type: string
          example: Doe
        role:
          type: string
          enum: [GUEST, HOST, ADMIN]
          example: GUEST
        isVerified:
          type: boolean
          example: true

    RegisterRequest:
      type: object
      required:
        - email
        - password
        - firstName
        - lastName
      properties:
        email:
          type: string
          format: email
          example: guest@example.com
        password:
          type: string
          format: password
          minLength: 8
          example: StrongPass123
        firstName:
          type: string
          example: Jane
        lastName:
          type: string
          example: Doe

    LoginRequest:
      type: object
      required:
        - email
        - password
      properties:
        email:
          type: string
          format: email
          example: guest@example.com
        password:
          type: string
          format: password
          example: StrongPass123

    RefreshRequest:
      type: object
      required:
        - token
      properties:
        token:
          type: string
          description: Refresh token returned from login or register.
          example: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

    VerifyEmailRequest:
      type: object
      required:
        - email
        - code
      properties:
        email:
          type: string
          format: email
          example: guest@example.com
        code:
          type: string
          minLength: 6
          maxLength: 6
          example: "123456"

    ForgotPasswordRequest:
      type: object
      required:
        - email
      properties:
        email:
          type: string
          format: email
          example: guest@example.com

    ResetPasswordRequest:
      type: object
      required:
        - email
        - code
        - password
      properties:
        email:
          type: string
          format: email
          example: guest@example.com
        code:
          type: string
          minLength: 6
          maxLength: 6
          example: "123456"
        password:
          type: string
          format: password
          minLength: 8
          example: NewStrongPass123

    AuthTokens:
      type: object
      required:
        - accessToken
        - refreshToken
        - user
      properties:
        accessToken:
          type: string
          example: eyJhbGciOi...
        refreshToken:
          type: string
          example: eyJhbGciOi...
        user:
          $ref: "#/components/schemas/User"

    Country:
      type: object
      required:
        - id
        - name
        - code
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          example: Tunisia
        code:
          type: string
          example: TN

    City:
      type: object
      required:
        - id
        - name
        - countryId
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          example: Tunis
        countryId:
          type: string
          format: uuid

    Currency:
      type: object
      required:
        - id
        - code
        - name
      properties:
        id:
          type: string
          format: uuid
        code:
          type: string
          example: TND
        name:
          type: string
          example: Tunisian Dinar

    Category:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          example: Apartment

    Unit:
      type: object
      required:
        - id
        - title
        - description
        - pricePerNight
        - maxGuests
        - cityId
        - categoryId
        - currencyId
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
          example: Cozy apartment in Tunis
        description:
          type: string
          example: Bright apartment near the old medina.
        pricePerNight:
          type: number
          format: float
          example: 120
        maxGuests:
          type: integer
          example: 4
        cityId:
          type: string
          format: uuid
        categoryId:
          type: string
          format: uuid
        currencyId:
          type: string
          format: uuid
        isActive:
          type: boolean
          example: true
        ownerId:
          type: string
          format: uuid

    Booking:
      type: object
      required:
        - id
        - unitId
        - guestId
        - checkIn
        - checkOut
        - totalPrice
        - status
      properties:
        id:
          type: string
          format: uuid
        unitId:
          type: string
          format: uuid
        guestId:
          type: string
          format: uuid
        checkIn:
          type: string
          format: date
        checkOut:
          type: string
          format: date
        totalPrice:
          type: number
          format: float
          example: 480
        status:
          type: string
          enum: [PENDING, CONFIRMED, REJECTED, CANCELLED, COMPLETED]
          example: PENDING

    Review:
      type: object
      required:
        - id
        - unitId
        - guestId
        - rating
      properties:
        id:
          type: string
          format: uuid
        unitId:
          type: string
          format: uuid
        guestId:
          type: string
          format: uuid
        rating:
          type: integer
          minimum: 1
          maximum: 5
          example: 5
        comment:
          type: string
          example: Great stay and very clean apartment.

    Favorite:
      type: object
      required:
        - userId
        - unitId
      properties:
        userId:
          type: string
          format: uuid
        unitId:
          type: string
          format: uuid

    ListResponse:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 200
        message:
          type: string
          example: Success
        data:
          type: array
          items:
            type: object

paths:
  /api/auth/register:
    post:
      tags: [Auth]
      summary: Register a new user
      description: Create a new Roovia account with email, password, and profile details.
      operationId: registerUser
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RegisterRequest"
            example:
              email: guest@example.com
              password: StrongPass123
              firstName: Jane
              lastName: Doe
      responses:
        "201":
          description: User created successfully
          content:
            application/json:
              schema:
                type: object
                required: [status, message, data]
                properties:
                  status:
                    type: integer
                    example: 201
                  message:
                    type: string
                    example: User created successfully
                  data:
                    $ref: "#/components/schemas/AuthTokens"
        "400":
          $ref: "#/components/responses/BadRequest"
        "409":
          $ref: "#/components/responses/Conflict"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/auth/login:
    post:
      tags: [Auth]
      summary: Login a user
      description: Authenticate a user and return access and refresh tokens.
      operationId: loginUser
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
            example:
              email: guest@example.com
              password: StrongPass123
      responses:
        "200":
          description: Login successful
          content:
            application/json:
              schema:
                type: object
                required: [status, message, data]
                properties:
                  status:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: User logged successfully
                  data:
                    $ref: "#/components/schemas/AuthTokens"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "500":
          $ref: "#/components/responses/InternalServerError"

  /api/auth/refresh:
    post:
      tags: [Auth]
      summary: Refresh access token
      description: Exchange a valid refresh token for a new access token.
      operationId: refreshToken
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RefreshRequest"
            example:
              token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
      responses:
        "200":
          description: Token refreshed successfully
          content:
            application/json:
              schema:
                type: object
                required: [status, message, accessToken]
                properties:
                  status:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: Token refreshed successfully
                  accessToken:
                    type: string
                    example: eyJhbGciOi...
        "400":
          $ref: "#/components/responses/BadRequest"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/auth/me:
    get:
      tags: [Auth]
      summary: Get current authenticated user
      description: Returns the currently authenticated user profile from the JWT payload.
      operationId: getCurrentUser
      security:
        - bearerAuth: []
      responses:
        "200":
          description: User retrieved successfully
          content:
            application/json:
              schema:
                type: object
                required: [status, message, data]
                properties:
                  status:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: User retrieved successfully
                  data:
                    $ref: "#/components/schemas/User"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/auth/verify-email:
    post:
      tags: [Auth]
      summary: Verify user email
      description: Confirm a user email address using the 6-digit OTP code generated during signup.
      operationId: verifyEmail
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VerifyEmailRequest"
            example:
              email: guest@example.com
              code: "123456"
      responses:
        "200":
          description: Email verified successfully
          content:
            application/json:
              schema:
                type: object
                required: [status, message, data]
                properties:
                  status:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: Email verified successfully
                  data:
                    $ref: "#/components/schemas/User"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/auth/forgot-password:
    post:
      tags: [Auth]
      summary: Request password reset code
      description: Send a password reset OTP to the user's email.
      operationId: forgotPassword
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForgotPasswordRequest"
            example:
              email: guest@example.com
      responses:
        "200":
          description: Password reset code sent successfully
          content:
            application/json:
              schema:
                type: object
                required: [status, message, data]
                properties:
                  status:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: Password changed successfully
                  data:
                    type: object
                    nullable: true
                    example: null
        "404":
          $ref: "#/components/responses/NotFound"

  /api/auth/reset-password:
    post:
      tags: [Auth]
      summary: Reset password
      description: Reset a user's password using the email and verification code.
      operationId: resetPassword
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ResetPasswordRequest"
            example:
              email: guest@example.com
              code: "123456"
              password: NewStrongPass123
      responses:
        "200":
          description: Password updated successfully
          content:
            application/json:
              schema:
                type: object
                required: [status, message, data]
                properties:
                  status:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: Password changed successfully
                  data:
                    $ref: "#/components/schemas/User"
        "400":
          $ref: "#/components/responses/BadRequest"
        "404":
          $ref: "#/components/responses/NotFound"

  /api/countries:
    get:
      tags: [Catalog]
      summary: List countries
      operationId: getCountries
      responses:
        "200":
          description: Countries fetched successfully
          content:
            application/json:
              schema:
                type: object
                required: [status, message, data]
                properties:
                  status:
                    type: string
                    example: "200"
                  message:
                    type: string
                    example: countries data fetched successfully
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Country"
    post:
      tags: [Catalog]
      summary: Create country
      operationId: createCountry
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, code]
              properties:
                name:
                  type: string
                  example: Tunisia
                code:
                  type: string
                  example: TN
      responses:
        "200":
          description: Country created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "200"
                  message:
                    type: string
                    example: country created successfully
                  data:
                    $ref: "#/components/schemas/Country"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/cities:
    get:
      tags: [Catalog]
      summary: List cities
      operationId: getCities
      responses:
        "200":
          description: Cities fetched successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "200"
                  message:
                    type: string
                    example: cities data fetched successfully
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/City"
    post:
      tags: [Catalog]
      summary: Create city
      operationId: createCity
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, countryId]
              properties:
                name:
                  type: string
                  example: Tunis
                countryId:
                  type: string
                  format: uuid
                  example: 2ebf420c-47e6-4a8f-87d2-9072f2ffdd1c
      responses:
        "200":
          description: City created successfully
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/currencies:
    get:
      tags: [Catalog]
      summary: List currencies
      operationId: getCurrencies
      responses:
        "200":
          description: Currencies fetched successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "200"
                  message:
                    type: string
                    example: currencies data fetched successfully
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Currency"

  /api/unit-categories:
    get:
      tags: [Catalog]
      summary: List categories
      operationId: getCategories
      responses:
        "200":
          description: Categories fetched successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: "200"
                  message:
                    type: string
                    example: categories data fetched successfully
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Category"
    post:
      tags: [Catalog]
      summary: Create category
      operationId: createCategory
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
                  example: Apartment
      responses:
        "200":
          description: Category created successfully
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/units:
    get:
      tags: [Units]
      summary: List units
      description: List publicly available units with optional query filters.
      operationId: listUnits
      parameters:
        - in: query
          name: cityId
          schema:
            type: string
            format: uuid
        - in: query
          name: categoryId
          schema:
            type: string
            format: uuid
        - in: query
          name: minPrice
          schema:
            type: number
            minimum: 0
        - in: query
          name: maxPrice
          schema:
            type: number
            minimum: 0
        - in: query
          name: page
          schema:
            type: integer
            minimum: 1
            default: 1
        - in: query
          name: limit
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 20
      responses:
        "200":
          description: Units listed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: units listed successfully
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Unit"
        "400":
          $ref: "#/components/responses/BadRequest"
    post:
      tags: [Units]
      summary: Create a unit
      operationId: createUnit
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Unit"
            example:
              title: Cozy apartment in Tunis
              description: Bright apartment near the old medina.
              pricePerNight: 120
              maxGuests: 4
              cityId: 2ebf420c-47e6-4a8f-87d2-9072f2ffdd1c
              currencyId: 8243b73b-1c7f-4f5e-bd59-0d3ff9d533a6
              categoryId: d3e78fbd-b3ea-468d-a337-9b3d5f7d91e2
      responses:
        "201":
          description: Unit created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 201
                  message:
                    type: string
                    example: unit created successfully
                  data:
                    $ref: "#/components/schemas/Unit"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/units/mine:
    get:
      tags: [Units]
      summary: List my units
      operationId: listMyUnits
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Units retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: units listed successfully
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Unit"

  /api/units/{id}:
    get:
      tags: [Units]
      summary: Get unit by id
      operationId: getUnitById
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Unit fetched successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: units fetched successfully
                  data:
                    $ref: "#/components/schemas/Unit"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags: [Units]
      summary: Update unit
      operationId: updateUnit
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Unit"
      responses:
        "200":
          description: Unit updated successfully
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    delete:
      tags: [Units]
      summary: Soft delete a unit
      operationId: softDeleteUnit
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Unit deleted successfully

  /api/units/{id}/activate:
    patch:
      tags: [Units]
      summary: Activate a unit
      operationId: activateUnit
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Unit activated successfully

  /api/units/{id}/deactivate:
    patch:
      tags: [Units]
      summary: Deactivate a unit
      operationId: deactivateUnit
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Unit deactivated successfully

  /api/bookings:
    post:
      tags: [Bookings]
      summary: Create a booking
      operationId: createBooking
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [unitId, checkIn, checkOut]
              properties:
                unitId:
                  type: string
                  format: uuid
                  example: 911f7b13-5d8e-4fca-bf16-8d39765d2f1d
                checkIn:
                  type: string
                  format: date
                  example: "2026-10-10"
                checkOut:
                  type: string
                  format: date
                  example: "2026-10-15"
      responses:
        "201":
          description: Booking created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 201
                  message:
                    type: string
                    example: booking created successfully
                  data:
                    $ref: "#/components/schemas/Booking"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/bookings/{id}:
    patch:
      tags: [Bookings]
      summary: Update booking dates
      operationId: updateBooking
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [checkIn, checkOut]
              properties:
                checkIn:
                  type: string
                  format: date
                  example: "2026-10-11"
                checkOut:
                  type: string
                  format: date
                  example: "2026-10-16"
      responses:
        "200":
          description: Booking updated successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: booking updated successfully
                  data:
                    $ref: "#/components/schemas/Booking"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    delete:
      tags: [Bookings]
      summary: Cancel booking
      operationId: cancelBooking
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Booking canceled successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: booking canceled successfully
                  data:
                    $ref: "#/components/schemas/Booking"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/bookings/{id}/confirm:
    patch:
      tags: [Bookings]
      summary: Confirm a booking as host
      operationId: confirmBooking
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Booking confirmed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: booking confirmed successfully
                  data:
                    $ref: "#/components/schemas/Booking"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/bookings/{id}/reject:
    patch:
      tags: [Bookings]
      summary: Reject a booking as host
      operationId: rejectBooking
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: id
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Booking rejected successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: booking rejected successfully
                  data:
                    $ref: "#/components/schemas/Booking"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/bookings/mine:
    get:
      tags: [Bookings]
      summary: Get my bookings as guest
      operationId: getGuestBookings
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Guest bookings retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: guest bookings retrieved successfully
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Booking"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/bookings/host:
    get:
      tags: [Bookings]
      summary: Get bookings for my units as host
      operationId: getHostBookings
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Host bookings retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: host bookings retrieved successfully
                  data:
                    type: array
                    items:
                      $ref: "#/components/schemas/Booking"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/units/{unitId}/reviews:
    get:
      tags: [Reviews]
      summary: Get unit reviews
      operationId: getUnitReviews
      parameters:
        - in: path
          name: unitId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Reviews returned successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: reviews fetched successfully
                  data:
                    type: object
                    properties:
                      reviews:
                        type: array
                        items:
                          $ref: "#/components/schemas/Review"
                      avgRating:
                        type: object
                        properties:
                          _avg:
                            type: object
                            properties:
                              rating:
                                type: number
                                nullable: true
                                example: 4.8
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      tags: [Reviews]
      summary: Create unit review
      operationId: createReview
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: unitId
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [rating]
              properties:
                rating:
                  type: integer
                  minimum: 1
                  maximum: 5
                  example: 5
                comment:
                  type: string
                  example: Great stay and very clean apartment.
      responses:
        "200":
          description: Review created successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: review created successfully
                  data:
                    $ref: "#/components/schemas/Review"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

  /api/units/{unitId}/favorite:
    post:
      tags: [Favorites]
      summary: Add unit to favorites
      operationId: addFavorite
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: unitId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Favorite added successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 201
                  message:
                    type: string
                    example: favorite created successfully
                  data:
                    $ref: "#/components/schemas/Favorite"
        "401":
          $ref: "#/components/responses/Unauthorized"
    delete:
      tags: [Favorites]
      summary: Remove unit from favorites
      operationId: removeFavorite
      security:
        - bearerAuth: []
      parameters:
        - in: path
          name: unitId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Favorite removed successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: favorite removed successfully
                  data:
                    $ref: "#/components/schemas/Favorite"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /api/favorites:
    get:
      tags: [Favorites]
      summary: List favorite units for the current user
      operationId: listFavorites
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Favorites retrieved successfully
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: integer
                    example: 200
                  message:
                    type: string
                    example: favorite fetched successfully
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        id:
                          type: string
                          format: uuid
                        userId:
                          type: string
                          format: uuid
                        unitId:
                          type: string
                          format: uuid
                        unit:
                          $ref: "#/components/schemas/Unit"
        "401":
          $ref: "#/components/responses/Unauthorized"
